apiVersion: v1
kind: Service
metadata:
  name: postgres-db-service
spec:
  type: ClusterIP  # Internal cluster communication only
  selector:
    app: postgres-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432